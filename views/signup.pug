extends layout

block content
        div.main-container
            div.form-container
                form(action='/users/signup' method='post')
                    h2 Sign Up
                    if(errors)
                        ul
                            each error in errors
                                li= error
                    input(type="hidden", name="_csrf", value=csrfToken)
                    .form-group
                        input.form-control(type="text", name="username", id="username" placeholder='Enter Username')
                        input.form-control(type="text", id='email', name='email' placeholder='Enter Email')
                        input.form-control(type="password", id='password', name='password' placeholder='Enter Password')
                        input.form-control(type="password", name="confirmPassword", id="confirmPassword" placeholder = 'Confirm Password')
                        input.form-control(type="text" name="avatar" placeholder="Enter an image URL")
                        input.form-button(type="submit" value='Sign Up')
                        a(href="/users/login").signup Already have an account?
